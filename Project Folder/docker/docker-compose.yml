version: '3.8'
services:
  door_inspec:
    image: door_inspec:latest
    container_name: door_inspec
    restart: unless-stopped
    network_mode: host
    environment:
      - SPOT_IP=${SPOT_IP:-192.168.80.3}
    logging:
      driver: journald